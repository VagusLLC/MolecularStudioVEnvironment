JOB_STORE:
  docs_store:
    type: MongoURIStore
    uri: {{MONGO_URI}}
    collection_name: docs_store
  additional_stores:
    files:
      type: GridFSURIStore
      database: data
      collection_name: files
      uri: {{MONGO_URI}}
    settings: 
      type: MongoURIStore
      uri: {{MONGO_URI}}
      database: data
      collection_name: settings
    properties:
      type: MongoURIStore
      uri: {{MONGO_URI}}
      database: data
      collection_name: properties